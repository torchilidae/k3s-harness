apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: gluetun-internal-certificate
  namespace: gluetun
spec:
  secretName: gluetun-internal-secret
  issuerRef:
    name: cloudflare-clusterissuer
    kind: ClusterIssuer
  commonName: ltc.tsphotoclicks.net
  dnsNames:
    - "ltc.tsphotoclicks.net"
    - "*.ltc.tsphotoclicks.net"